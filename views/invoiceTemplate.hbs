<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Invoice INV-{{orderId}}</title>
  <!-- Load Roboto from Google Fonts – reliable, modern, supports ₹ symbol -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', Arial, Helvetica, sans-serif;
      margin: 0;
      padding: 50px;
      color: #333;
      background: #fff;
      line-height: 1.5;
    }
    .header {
      text-align: center;
      margin-bottom: 50px;
    }
    .header h1 {
      margin: 0;
      font-size: 32px;
      color: #2c3e50;
      font-weight: 700;
    }
    .header p {
      margin: 10px 0 0;
      font-size: 16px;
      color: #555;
    }
    .info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 50px;
      font-size: 15px;
    }
    .section {
      width: 48%;
    }
    .section h3 {
      margin-bottom: 12px;
      border-bottom: 2px solid #3498db;
      padding-bottom: 6px;
      color: #2c3e50;
      font-weight: 500;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 40px;
      font-size: 15px;
    }
    th, td {
      padding: 14px 12px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #f8f9fa;
      font-weight: 500;
      color: #2c3e50;
    }
    .text-right {
      text-align: right;
    }
    .total-row td {
      font-weight: 500;
      background-color: #f8f9fa;
    }
    .grand-total td {
      font-size: 18px;
      font-weight: 700;
      color: #2c3e50;
      background-color: #eef5ff !important;
    }
    .text-success {
      color: #27ae60;
    }
    .footer {
      text-align: center;
      margin-top: 80px;
      color: #777;
      font-size: 14px;
      padding-top: 20px;
      border-top: 1px solid #eee;
    }
  </style>
</head>
<body>

  <div class="header">
    <h1>INVOICE</h1>
    <p><strong>Invoice No:</strong> INV-{{orderId}} &nbsp;&nbsp;|&nbsp;&nbsp; <strong>Date:</strong> {{date}}</p>
  </div>

  <div class="info">
    <div class="section">
      <h3>From</h3>
      <strong>BASHA BOOK</strong><br>
      Park Avenue<br>
      Chennai - 600034<br>
      Tamil Nadu, India<br>
      Email: support@bashabook.com
    </div>
    <div class="section">
      <h3>Bill To</h3>
      <strong>{{customerName}}</strong><br>
      {{addressLine1}}<br>
      {{#if addressLine2}}{{addressLine2}}<br>{{/if}}
      {{city}}, {{state}} - {{pin}}<br>
      Mobile: {{mobile}}<br>
      India
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th>Item Description</th>
        <th class="text-right">Unit Price</th>
        <th class="text-right">Quantity</th>
        <th class="text-right">Amount</th>
      </tr>
    </thead>
    <tbody>
      {{#each products}}
      <tr>
        <td>{{this.description}}</td>
        <td class="text-right">₹{{this.price}}</td>
        <td class="text-right">{{this.quantity}}</td>
        <td class="text-right">₹{{multiply this.price this.quantity}}</td>
      </tr>
      {{/each}}

      {{#if hasDiscount}}
      <tr class="total-row">
        <td colspan="3" class="text-right">Discount</td>
        <td class="text-right text-success">-₹{{discountAmt}}</td>
      </tr>
      {{/if}}

      <tr class="grand-total">
        <td colspan="3" class="text-right"><strong>Grand Total</strong></td>
        <td class="text-right"><strong>₹{{grandTotal}}</strong></td>
      </tr>
    </tbody>
  </table>

  <div class="footer">
    Thank you for shopping with <strong>BASHA BOOK</strong>!<br>
    For any queries, feel free to contact us at <strong>support@bashabook.com</strong>
  </div>

</body>
</html>